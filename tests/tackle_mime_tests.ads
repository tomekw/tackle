with Testy.Tests;

package Tackle_MIME_Tests is
   use Testy.Tests;

   procedure Test_MIME_Exists (T : in out Test_Context);

   procedure Test_MIME_Does_Not_Exist (T : in out Test_Context);

   procedure Test_MIME_Does_Not_Exist_With_Default (T : in out Test_Context);
end Tackle_MIME_Tests;
